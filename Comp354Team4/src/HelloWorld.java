import java.util.ArrayList;
import java.util.Calendar;
import java.util.List;

import data.*;
import models.*;
public class HelloWorld {
	
	public static void main (String[] args) {
		System.out.println("Hello World!");
		
		
		System.out.println("Hello World2!");
		
		//Mouad : I added this to test if commits form Mac will work on PC
		System.out.println("Hello World3!");
		
		//SqliteSetup setup = new SqliteSetup();
		//setup.init();
		//setup.executeSql(ProjectDao.CreateTable);
		//Project test = new Project();
		
		//test.setProjectName("Test Project 1");
		//test.setStartDate(Calendar.getInstance().getTime());
		//test.setEndDate(Calendar.getInstance().getTime());
		//test.setManagerId(1);
		
		//ProjectDao dao = new ProjectDao();
		//dao.InsertProject(test);		

		//List<Project> projects = dao.GetProjectsGivenManagerId(1);
		
		//return;
		
	}
}
